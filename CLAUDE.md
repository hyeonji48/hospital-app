# Hospital Navigation Assistant

서비스명 **동행온** — "어르신의 발걸음에 따뜻함(溫)을 더하다".
**부천시 혁신나래연구단** 출품작. **가톨릭대학교 부천성모병원 성모관 1F·2F·4F**를 시범 대상으로 한
병원 내 동선 안내 웹서비스 프로토타입.

대회 계보: CUK 청년리더 프로그램(완료) → **혁신나래연구단(현재 출전)**. 공무원 논의 내용 반영.

브랜드 컬러: `#3F477F` (남보라) / active `#2C3159` / tint `#E8EAF4`.
실제 부천성모 층별 부서 데이터: `docs/BUCHEON_CMC_FLOORS.md`

## 세션 시작 시 반드시 읽을 것

- **`docs/PROJECT_BRIEF.md`** — 대회 평가기준, 확정 결정사항, 미결정 사항, 진행 상태
- **`docs/CODEBASE_AUDIT.md`** — 스택·목데이터 구조·갭 분석·마이그레이션 전략 (2026-08-23 실사)
- **`docs/BUCHEON_CMC_FLOORS.md`** — 부천성모 실제 층별 부서 + 답사 체크리스트

## 제품 콘셉트 (2026-08-23 확정)

**"접수증이 곧 경로가 됩니다. 검색할 필요가 없습니다."**

키오스크에서 접수하면 접수증에 오늘 할 일이 순서대로 인쇄된다. 환자는 그 종이를 찍기만 하고,
앱이 목록을 따라 순차 안내한다. 기존 병원 앱은 "정형외과"를 검색할 줄 아는 사람용이지만,
그걸 아는 사람은 애초에 덜 헤맨다 — 이 차이가 창의성 30점의 방어 논거다.

핵심 주장은 **"정보 제공 UX → 행동 확신 UX"**, 그리고 AI의 역할은
**병원 용어 → 행동어 변환** ("채혈실" → "피 뽑는 곳"). 근거: 경희의료원 현장 인터뷰 +
노인실태조사 45.6%(도움 요청을 참은 노인 비율) + 논문 4편.

진입은 **NFC 태그 → 웹** (앱 설치·회원가입·사전예약 전면 배제 = Zero-Barrier).
NFC는 환자 정보를 읽는 기술이 아니라 웹페이지를 여는 접근성 장치일 뿐. **NFC 스티커 구매 완료.**

- ❌ **"최소 체류시간 순서 최적화" 스케줄러는 폐기됨.** 다시 제안하지 말 것.
- ❌ **라우팅 엔진(Dijkstra/A*)도 보류.** 피치가 "길찾기보다 다음 행동"이라 선언한 영역에
  개발 시간을 쓰는 것이므로. 손으로 찍은 `pathPoints`로 시연은 충분하다.
- ✅ **접수증 사진 → 행동 리스트 AI 변환 구현됨** (`api/transform.ts` + `src/app/lib/noticeTransform.ts`).
  **아직 UI에 배선되지 않았다** — 화면은 여전히 `TASKS` 하드코딩을 쓴다.
- 별도 OCR 서비스(CLOVA/Google Vision)를 쓰지 않는다. Gemini가 사진을 직접 읽는다.
  붙일 서비스와 실패 지점이 하나씩 줄어든다.
- ✅ **라우팅 엔진 구현됨** (`router.ts`). 병원 공식 평면도에서 옮긴 통로 그래프 + Dijkstra.
- ✅ **두 가지 모드**: `demo`(저장된 결과, 네트워크 안 탐) / `live`(실제 AI 인식).
  **코드는 하나이고 데이터 출처만 다르다.** 첫 화면에서 고른다. URL은 바뀌지 않는다 —
  NFC 태그에 적힌 주소를 그대로 써야 하기 때문.

## 도면에 대해 (착각 주의)

병원 공식 홈페이지 `병원둘러보기`에 **층별 평면도 PNG**가 있다 (820×660).
`floorPlan.ts` 좌표는 그것을 0.439배 축소해 옮긴 것이다. 저작권 때문에 **원본 이미지는
레포에 넣지 않는다.**

- 실제 성모관은 **중앙 코어(엘리베이터 6대 + 계단 2 + 화장실)를 방들이 둘러싼 2차원 배치**다.
  "곧은 복도 하나 + 위아래 두 줄"이 아니다. 통로는 `WAYPOINTS`/`LINKS`로 직접 그린다.
- 도면이 축척이라 **거리는 좌표로 계산된다. 걸음 수를 잴 필요가 없다.**
- ⚠️ **원무과·수납창구·안내데스크는 평면도에 없다.** 추정 배치이며 `estimated: true` 표시.
  답사 최우선 확인 대상.
- 소요시간(`estimatedTime`)은 계산은 하지만 **화면에 표시하지 않는다.** 실측 보정을 안 했으므로
  발표 숫자로 인용하지 말 것. 그 숫자는 답사 때 스톱워치로 잰다.

## 의사 이름을 다루지 않는다

예전 `ConfirmScreen`에 있던 "정형외과 김멋사 교수님" 하드코딩은 제거됐다.
장소는 `placeIds` enum으로 못박아 환각을 막을 수 있지만 **사람 이름은 그렇게 막을 수 없다.**
AI 프롬프트도 "환자 이름·생년월일·등록번호를 결과에 담지 말 것"으로 지시한다.
어르신에게 필요한 것은 "누가 보는가"가 아니라 "어디로 가는가"다.

**AI 변환 설계 원칙 — 장소도 결정론적으로:**
LLM이 만들어내는 것은 *행동 문장*과 *어느 장소인지*뿐이다. 건물·층·공식명칭은 전부
`places.ts` 사전이 채운다. `placeId`가 zod enum으로 못박혀 있어 **부천성모에 없는 진료실을
지어내는 것이 스키마 수준에서 불가능**하다. 이 제약을 절대 풀지 말 것.
- ⚠️ 접수증 스캔이 핵심 주장의 유일한 증거물인데 **현재 가짜다** (`CameraView`의 `setTimeout(1800)`).
  실물 접수증의 QR·바코드 유무를 현장답사에서 확인한 뒤 구현 범위를 정한다. **답사 전에
  OCR 개발을 시작하지 말 것.**

## 판단 기준 (이 프로젝트의 헌법)

모든 제안은 **100점 평가기준표의 어느 항목 몇 점에 대응하는지와 함께** 설명한다.

| 활용가능성 40 | 창의성 30 | 충실성 30 |
|---|---|---|
| 실현가능성 15 / 확장성 15 / 공공성 10 | 독창성 15 / 참신성 15 | 참여도 15 / 완성도 15 |

- 대상 기관은 **부천성모병원** (서울성모 아님). 부천시 소재여야 실현가능성 15점이 성립한다.
- 코드베이스는 **하나만** 유지한다. 시연용/실서비스는 데이터 레이어로 분리한다.
- 범위 확장을 제안할 때는 **잔여 기간 대비 리스크를 함께** 말한다. 해커톤에서 가장 큰 실패는
  "완성하지 못하는 것"이다.
- 사용자의 초안에 위험이 있으면 **직접적으로 지적한다.** 결론만 던지지 말고 트레이드오프를 같이 낸다.

## 기술 원칙

- **경로는 결정론적으로, 말은 LLM으로.** 경로 계산은 Dijkstra/A*. LLM에 맡기면 존재하지 않는
  복도를 만들어내며, 하필 시연 중에 그런 일이 발생한다.
- LLM은 자연어→목적지 매핑, 순서 추천 사유 설명, 다국어 안내에만 쓴다.
- 시연 경로에서 네트워크/실시간 API 의존을 최소화한다. 호출 결과는 사전 캐싱하고 폴백 문구를 둔다.

## 스택

React 18 + Vite 6 + TypeScript + Tailwind v4 + motion/react + shadcn/ui(Radix).
백엔드 없음, 라우터 없음, 상태관리 라이브러리 없음. Figma Make 익스포트가 원본.

```bash
npm i && npm run dev
```

**배포:** Vercel — <https://hospital-app-xi-sooty.vercel.app/>
(레포: `github.com/hyeonji48/hospital-app`. 예선덱에 적힌 netlify 주소는 폐기됨.)

| 명령 | 용도 |
|---|---|
| `npm run dev` | 프론트만 (5173). 서버리스 함수는 안 뜸 |
| `npm run dev:api` | `vercel dev` — 함수 포함 전체 |
| `npm run typecheck` | `tsc --noEmit` |
| `npm run gen:fixture -- "<안내문>"` | 실제 API 응답을 시연 캐시로 굳히기 |
| `npm run gen:speech` | 시연용 음성 파일 생성 (Gemini TTS) |
| `npm run try:phone` | **폰에서 테스트할 때.** 빌드본을 같은 와이파이에 공개 |

⚠️ **`npm run dev -- --host` 로 폰 테스트를 하지 말 것.** 개발 서버는 소스 파일과
HMR 웹소켓까지 같이 열려서, 같은 와이파이에 있는 사람이 파일을 읽을 수 있는 취약점이
Vite 에 반복적으로 나온다 (6.3.5 에 실제로 high 등급 하나 있었고 6.4.3 에서 수정됨).
꼭 로컬에서 폰 테스트를 해야 하면 `npm run try:phone` 을 쓴다 — 빌드된 정적 파일만
서빙하므로 노출 면적이 훨씬 작다. **가장 안전한 방법은 그냥 Vercel 에 배포해서
실제 주소로 보는 것**이고, 어차피 심사장에서 쓸 것도 그 주소다.

**API 키:** `GEMINI_API_KEY`는 Vercel 환경변수에만 둔다. `VITE_` 접두사를 붙이면
번들에 그대로 박혀 노출되므로 절대 금지. 공개 URL이라 키가 새면 우리 요금이 나간다.

**AI 제공사: Google Gemini 무료 티어** (2026-08-24 결정). 예산 0원 제약 때문.
- 무료 티어는 입력이 구글 모델 학습에 사용된다 → **그래서 실제 접수증을 절대 쓰지 않는다.**
  자체 제작 샘플(`public/sample-receipt.html`)만 사용한다. 이 둘은 세트로 묶인 결정이다.
- 실서비스 단계에서는 유료 티어로 전환해야 개인정보가 학습에서 제외된다. 발표 답변에 포함할 것.
- 제공사 교체는 `api/transform.ts` 한 파일. 화면·사전·캐시·폴백은 안 바뀐다.

## 코드 지도

| 경로 | 역할 |
|---|---|
| `src/app/App.tsx` | 상태머신 `camera→loading→confirm→navigating→arrived→complete` + 아이폰 목업 프레임 |
| `src/app/types.ts` | **목데이터 전체.** `TASKS: Task[]` 6개, 각 `NavStep`에 손으로 찍은 `pathPoints` |
| `src/app/components/HospitalFloorMap.tsx` | SVG 층별 지도. `FLOOR_1~4_ROOMS` 하드코딩 + 진행방향 자동회전/자동줌 |
| `src/app/components/NavigationScreen.tsx` | 길안내 본체, 전체화면 약도, 엘리베이터 패널 |
| `src/app/hooks/useTTS.ts` | Web Speech API, `ko-KR`, rate 0.9 (고령자용) |
| `src/app/components/ui/` | shadcn/ui 전체 복사본. 대부분 미사용 — 건드리지 말 것 |
| `src/app/data/places.ts` | **부천성모 성모관 장소 사전 27곳.** AI가 고를 수 있는 장소의 전부 |
| `src/app/data/floorPlan.ts` | **공식 평면도 기반 도면 + 통로 그래프.** 좌표계 360×290 |
| `src/app/lib/router.ts` | Dijkstra 최단경로 → `NavStep[]`. 결정론적, AI 아님 |
| `src/app/lib/buildTasks.ts` | AI 행동 리스트 → 화면용 `Task[]` |
| `src/app/components/ModeSelect.tsx` | 첫 화면. 체험(demo) / 실제 인식(live) |
| `src/app/lib/noticeTransform.ts` | 안내문 → 행동 리스트. 캐시 → API → 폴백 3단 |
| `src/app/data/noticeFixtures.ts` | 시연용 사전 캐싱 결과 |
| `api/transform.ts` | Vercel Function. **Gemini 호출 + API 키 보관 + 남용 방지.** AI를 부르는 유일한 곳 |
| `public/sample-receipt.html` | 인쇄용 샘플 접수증 3종. 실제 접수증 대신 이것만 쓴다 |

## 핵심 구조 사실 (착각하기 쉬움)

1. **라우팅 엔진이 없다.** `pathPoints`는 사람이 손으로 찍은 폴리라인이고, 현재 앱은
   미리 만든 슬라이드쇼다. 그래프 탐색 도입은 "데이터 교체"가 아니라 **신규 구현**이다.
2. **좌표계는 SVG viewBox `360 × 240`.** 방(`x,y,w,h`)과 경로점(`[x,y]`)이 같은 공간을 쓴다.
   → 브리핑 §6의 노드 스키마를 이 공간에 그대로 얹을 수 있다.
3. **`HospitalFloorMap`의 입력은 `NavStep` 하나뿐이다.** 라우터가 `NavStep[]`만 생산하면
   지도·화면·TTS·애니메이션을 전부 재사용할 수 있다. `NavStep`을 경계 인터페이스로 고정할 것.
4. **방↔경로 연결이 문자열 일치다.** `HospitalFloorMap.tsx:259`의
   `step.targetRoom === room.label`. 오타 하나면 조용히 강조가 사라진다. ID 참조로 교체 대상.
5. **`getFloorRooms`의 폴백이 위험하다.** 1/2/3층이 아니면 전부 4층 도면을 반환하므로
   지하1층(`floor: -1` 또는 `0`)이 4층으로 그려진다. B1 지원 시 반드시 수정.
6. **`CameraView`의 스캔은 가짜다.** 실제 카메라는 켜지지만 OCR 없이 `setTimeout(1800)`이다.
   제품 콘셉트가 접수증 기반으로 확정된 이상 **이것이 최우선 기술 리스크**다.
7. **`ConfirmScreen`에 "정형외과 김멋사 교수님"이 TTS 문구까지 하드코딩돼 있다.**
   시연 시나리오(순환기내과 어르신)와 불일치 — 대본 확정 시 여기부터 교체.

## 커밋 규칙

현장답사·회의 산출물은 버리지 않는다. **커밋 로그도 충실성 참여도 15점의 채점 대상**이므로
작업 단위를 잘게 쪼개고 메시지를 한국어로 명확히 남긴다.
